import random

low = 1
high = 100
options=["rock", "paper", "scissors"]
cards = ["Ace", "2", "3", "4", "5", "6", "7", "8", "9", "10", "Jack", "Queen", "King"]

# num = random.randint(low, high)  #random whole number between low and high
# num = random.random() #random float between low and high
# option = random.choice(options) # randomly choose an item from a list
# card = random.shuffle(cards) # randomly shuffle a list


print(option)